# docs/

| document | contents |
|---|---|
| [`model.md`](model.md) | notation, units, entity definitions and the exact definition of every reported metric |
| [`architecture.md`](architecture.md) | why the code is shaped the way it is: purity of the engine, two backends over one behavioural model, constraint handling, how noise is treated, provenance, extension points |

The project README is the entry point; these two go a level deeper on the
questions a reviewer would ask second.
